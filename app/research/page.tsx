import {Header,Footer} from '../components';import {researchPosts} from '../fleet-content';
export const metadata={title:'Research',description:'Sourced research notes about Philippines-based payroll operations.'};
export default function Research(){return <><Header/><main className="fleet-main"><section className="fleet-hero"><div className="container"><p className="eyebrow">Research library</p><h1>Research for planning Philippines-based payroll operations</h1><p className="lead">Ten dated, source-led briefs covering controls, handoffs, access, reconciliation, and review routines.</p></div></section><section className="section"><div className="container fleet-card-grid">{researchPosts.map(p=><a className="fleet-card" href={`/research/${p.slug}`} key={p.slug}><p className="eyebrow">{p.published}</p><h2>{p.title}</h2><p>{p.excerpt}</p></a>)}</div></section></main><Footer/></>}
